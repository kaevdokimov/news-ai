<!DOCTYPE html>
<html>
<head>
    <title>Панель администратора News AI</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                    <div class="container-fluid">
                        <a class="navbar-brand" href="/admin">
                            <i class="fas fa-newspaper"></i> News AI Admin
                        </a>
                        <div class="navbar-nav ms-auto">
                            <a class="nav-link" href="{{ path('admin_sources') }}">
                                <i class="fas fa-rss"></i> Источники
                            </a>
                            <a class="nav-link" href="{{ path('admin_news') }}">
                                <i class="fas fa-newspaper"></i> Новости
                            </a>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
        
        <div class="container mt-4">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header bg-primary text-white">
                            <h3 class="card-title mb-0">
                                <i class="fas fa-tachometer-alt"></i> Добро пожаловать в панель администратора News AI
                            </h3>
                        </div>
                        <div class="card-body">
                            <p class="lead">Здесь вы можете управлять источниками новостей и просматривать собранные новости.</p>
                            
                            <div class="row mb-4">
                                <div class="col-md-6">
                                    <div class="card border-primary">
                                        <div class="card-body text-center">
                                            <i class="fas fa-rss fa-3x text-primary mb-3"></i>
                                            <h5 class="card-title">Источники новостей</h5>
                                            <p class="card-text">Управление RSS лентами</p>
                                            <h3 class="text-primary">{{ sources|length }}</h3>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="card border-success">
                                        <div class="card-body text-center">
                                            <i class="fas fa-newspaper fa-3x text-success mb-3"></i>
                                            <h5 class="card-title">Новости</h5>
                                            <p class="card-text">Просмотр собранных новостей</p>
                                            <h3 class="text-success">{{ newsCount }}</h3>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="row">
                                <div class="col-12">
                                    <div class="card border-info">
                                        <div class="card-header bg-info text-white">
                                            <h4 class="card-title mb-0">
                                                <i class="fas fa-bolt"></i> Быстрые действия
                                            </h4>
                                        </div>
                                        <div class="card-body">
                                            <a href="{{ path('admin_sources') }}" class="btn btn-primary btn-lg me-3">
                                                <i class="fas fa-rss"></i> Управление источниками
                                            </a>
                                            <a href="{{ path('admin_news') }}" class="btn btn-success btn-lg">
                                                <i class="fas fa-newspaper"></i> Просмотр новостей
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            {% if recentNews|length > 0 %}
                            <div class="row mt-4">
                                <div class="col-12">
                                    <div class="card border-warning">
                                        <div class="card-header bg-warning text-dark">
                                            <h4 class="card-title mb-0">
                                                <i class="fas fa-clock"></i> Последние новости
                                            </h4>
                                        </div>
                                        <div class="card-body">
                                            <div class="row">
                                                {% for item in recentNews|slice(0, 6) %}
                                                <div class="col-md-6 mb-3">
                                                    <div class="card">
                                                        <div class="card-body">
                                                            <h6 class="card-title">{{ item.title|slice(0, 80) }}{% if item.title|length > 80 %}...{% endif %}</h6>
                                                            <p class="card-text">
                                                                <small class="text-muted">
                                                                    <i class="fas fa-calendar"></i> {{ item.publishedAt|date('d.m.Y H:i') }}
                                                                    <br>
                                                                    <i class="fas fa-rss"></i> {{ item.source.name }}
                                                                </small>
                                                            </p>
                                                        </div>
                                                    </div>
                                                </div>
                                                {% endfor %}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
