<!DOCTYPE html>
<html>
<head>
    <title>Источники новостей - News AI Admin</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                    <div class="container-fluid">
                        <a class="navbar-brand" href="/admin">
                            <i class="fas fa-newspaper"></i> News AI Admin
                        </a>
                        <div class="navbar-nav ms-auto">
                            <a class="nav-link" href="{{ path('admin_sources') }}">
                                <i class="fas fa-rss"></i> Источники
                            </a>
                            <a class="nav-link" href="{{ path('admin_news') }}">
                                <i class="fas fa-newspaper"></i> Новости
                            </a>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
        
        <div class="container mt-4">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header bg-primary text-white">
                            <h3 class="card-title mb-0">
                                <i class="fas fa-rss"></i> Источники новостей
                            </h3>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Название</th>
                                            <th>URL</th>
                                            <th>Статус</th>
                                            <th>Последний парсинг</th>
                                            <th>Создан</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for source in sources %}
                                        <tr>
                                            <td>{{ source.id }}</td>
                                            <td>
                                                <strong>{{ source.name }}</strong>
                                                {% if source.description %}
                                                <br><small class="text-muted">{{ source.description }}</small>
                                                {% endif %}
                                            </td>
                                            <td>
                                                <a href="{{ source.url }}" target="_blank" class="text-decoration-none">
                                                    {{ source.url|slice(0, 50) }}{% if source.url|length > 50 %}...{% endif %}
                                                    <i class="fas fa-external-link-alt"></i>
                                                </a>
                                            </td>
                                            <td>
                                                {% if source.isActive %}
                                                    <span class="badge bg-success">Активен</span>
                                                {% else %}
                                                    <span class="badge bg-secondary">Неактивен</span>
                                                {% endif %}
                                            </td>
                                            <td>
                                                {% if source.lastParsedAt %}
                                                    {{ source.lastParsedAt|date('d.m.Y H:i') }}
                                                {% else %}
                                                    <span class="text-muted">Никогда</span>
                                                {% endif %}
                                            </td>
                                            <td>{{ source.createdAt|date('d.m.Y H:i') }}</td>
                                        </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                            
                            {% if sources|length == 0 %}
                            <div class="alert alert-info">
                                <i class="fas fa-info-circle"></i> Источники новостей не найдены.
                            </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
