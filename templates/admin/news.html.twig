<!DOCTYPE html>
<html>
<head>
    <title>Новости - News AI Admin</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                    <div class="container-fluid">
                        <a class="navbar-brand" href="/admin">
                            <i class="fas fa-newspaper"></i> News AI Admin
                        </a>
                        <div class="navbar-nav ms-auto">
                            <a class="nav-link" href="{{ path('admin_sources') }}">
                                <i class="fas fa-rss"></i> Источники
                            </a>
                            <a class="nav-link" href="{{ path('admin_news') }}">
                                <i class="fas fa-newspaper"></i> Новости
                            </a>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
        
        <div class="container mt-4">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header bg-success text-white">
                            <h3 class="card-title mb-0">
                                <i class="fas fa-newspaper"></i> Новости ({{ news|length }})
                            </h3>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                {% for item in news %}
                                <div class="col-md-6 mb-4">
                                    <div class="card h-100">
                                        {% if item.imageUrl %}
                                        <img src="{{ item.imageUrl }}" class="card-img-top" alt="{{ item.title }}" style="height: 200px; object-fit: cover;">
                                        {% endif %}
                                        <div class="card-body">
                                            <h5 class="card-title">{{ item.title }}</h5>
                                            {% if item.description %}
                                            <p class="card-text">{{ item.description|slice(0, 150) }}{% if item.description|length > 150 %}...{% endif %}</p>
                                            {% endif %}
                                            <div class="d-flex justify-content-between align-items-center">
                                                <small class="text-muted">
                                                    <i class="fas fa-calendar"></i> {{ item.publishedAt|date('d.m.Y H:i') }}
                                                    <br>
                                                    <i class="fas fa-rss"></i> {{ item.source.name }}
                                                </small>
                                                {% if item.link %}
                                                <a href="{{ item.link }}" target="_blank" class="btn btn-sm btn-outline-primary">
                                                    <i class="fas fa-external-link-alt"></i> Читать
                                                </a>
                                                {% endif %}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                {% endfor %}
                            </div>
                            
                            {% if news|length == 0 %}
                            <div class="alert alert-info">
                                <i class="fas fa-info-circle"></i> Новости не найдены. Запустите парсинг источников.
                            </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
